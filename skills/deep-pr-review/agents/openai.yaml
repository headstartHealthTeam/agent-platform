interface:
  display_name: 'Deep PR Review'
  short_description: 'Run a defensible, evidence-backed PR review'
  default_prompt: 'Use $deep-pr-review to review this pull request and report only validated findings.'
policy:
  allow_implicit_invocation: true
